Read the full process at docs/prompts/FIX.md and execute it.

Error or bug to fix: $ARGUMENTS

Paste the full error log and the relevant file contents before the surgical protocol begins. Do not attempt a fix without both.

Fix only what the error requires. Flag anything else found during the fix as a separate issue — do not clean up surrounding code in the same pass.
