# Codebase Audit — 2026-06-13

**Cycle:** 32 · **Build status:** ✅ (last confirmed E121 close, 2026-06-12)

---

## 1. Current State

**Last closed:** E121 · Home Page Hero Redesign (2026-06-12). The Cycle Goal (E107 + E109 + E110 — POS Loan Issuance & Compliance) is marked COMPLETED. The In Progress table is **empty**; there is no active work.

Cycle 32 was extremely productive: 20+ epics closed, covering the full pawn loan lifecycle (digital signatures, walk-in intake, APR compliance, printed tickets, receipt emails), inventory photo management, a new edit-item page, home hero redesign, and several P0 bugfixes.

---

## 2. Schema vs Code Drift

No significant drift found. `types.ts` and `firestore-schema.md` are in alignment for all active collections.

**Minor gap:** `aiTitle` and `aiCategory` are in `firestore-schema.md` under `items/{id}/internal/ai` (added E97) and referenced in code — confirmed consistent.

No dead-code schema fields identified in this scan.

---

## 3. Open Risks

**`any` type violations (2):**
- `src/pages/admin/LoanTicketsAdminPage.tsx:132–134` — `const tableColumns: any` and `const tableData: any` are direct guardrail violations. Low blast radius but must be typed.
- `src/components/auth/AcknowledgmentWall.tsx:19` — `(window as any).__PLAYWRIGHT_MOCK_USER__` is test scaffolding acceptable in context; not a production risk.

**Hardcoded px/rem values (low severity):**
- `ProfilePage.tsx:139` — `width: '1.25rem', height: '1.25rem'` on a checkbox. Should use `--space-5` or a size token.
- `FireworksPage.tsx:52,77`, `BundleCollectionPage.tsx:60,84`, `ItemDetailPage.tsx:108`, `InventoryPage.tsx:530,550`, `StoryStrip.tsx:8` — `minmax(280px, ...)` and `minmax(320px, ...)` in `gridTemplateColumns`. These are layout column thresholds, not spacing/font tokens. Not in the `--space-*` token set — note in next QA pass.

**No TODOs or FIXMEs** found in production source.

**Age gates:** All at router level in `main.tsx` (cannabis 19+, fireworks/tobacco 19+). No component-level gates detected. ✅

---

## 4. Recommended Next Step

The sprint is clean with no in-flight work. Three clear candidate tracks:

1. **E79 · Stripe Integration** (Phase 23, `CRITICAL` priority) — marked as the revenue enablement gate. No payment infrastructure exists yet; blocks E80 (POS), E86 (Loyalty), E89 (Tax receipts). Start with `/plan stripe integration`.

2. **E68-QA · Secret Manager Production Provisioning** (Phase 18) — all 5 production secrets (SendGrid, Twilio, eBay, Brother POS, Backup Bucket) are still using dummy keys. This is a hard gate before any prod launch. Config-only work; no code required.

3. **`any` type fix in `LoanTicketsAdminPage.tsx`** — 2-line surgical fix. Run `/fix` against lines 132–134.

**Highest priority:** E68-QA unblocks prod; E79 unlocks revenue. Start a `/plan` for E79 if there is developer capacity, and close E68-QA in parallel as a config operation.
