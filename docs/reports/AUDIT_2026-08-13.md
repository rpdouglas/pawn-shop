# Audit — 2026-08-13

## 1. Current State

Cycle 33 (started 2026-05-22) has no target close date and no open "In Progress" row — the last six items (E107, E109, E110, E122, E124, E125, E126, E127, plus two hero-space fixes) are all logged **COMPLETED**. The codebase is effectively between cycles: the last committed work (merged PR #7, 2026-08-13 in this branch's history) removed a `minHeight: 80vh` floor on `PawnHero.tsx`. Cannabis vertical is under a deliberate legal-hold suspension (E123) — routes, nav, and homepage card are commented out, source preserved. Last known CI status per `ACTIVE_CYCLE.md` entries: Build ✅ Lint ✅ Tests 29/29 ✅ CF `tsc` ✅. This session's own `tsc -b` couldn't verify — `node_modules` isn't installed in this container.

## 2. Schema vs Code Drift

None found. `Item`/`LoanTicket`/`PawnRequest` in `types.ts` line up with `firestore-schema.md` field-for-field, including the newer `source`, `aprOverrideConfirmed`, `receiptEmailSentAt` fields. `types.ts` even has one field ahead of the doc — `MerchandisingTag` includes `'price-dropped'`, not yet listed in `firestore-schema.md`'s `merchandisingTags` row. Minor doc gap, not a functional drift.

## 3. Open Risks

- **No `TODO`/`FIXME`** in `src` or `functions/{core,operations}`.
- **3 `any` usages**, all narrow: a Playwright test-mock cast, and two React Table generic casts in `LoanTicketsAdminPage.tsx:132,134` — guardrail violation, small footprint.
- **10 files with hardcoded hex values**, incl. `IssueLoanModal.tsx`, `LuxuryProductCard.tsx`, `CannabisPage.tsx`, `ViewLayout.tsx` — guardrail says "never hardcode hex," worth a token-sweep epic.
- **~97 files** with raw `px` in JSX — likely includes legitimate cases (border-radius, 1px hairlines) alongside real token violations; needs manual triage, not a blind sweep.
- **Age gates remain router-level** — `main.tsx` wraps `/fireworks`, `/tobacco`, and `ItemDetailPage.tsx` wraps dynamic item routes correctly. No component-level bypass found.

## 4. Recommended Next Step

Two live blockers sit in `Deferred/Blocked`: **MFA-bypass-impossible** and **eBay dev account setup**, both gating pre-prod work, both outside codebase scope (Identity Platform upgrade; eBay seller credentials). Legal counsel on cannabis/fireworks regulation is still the single **Open Decision** blocking E123's reversal and any prod push.

Since Cycle 33 has no open work and no stated goal, the highest-priority *codebase* action is closing the cycle: log a Cycle 34 goal in `ACTIVE_CYCLE.md`, and pick up the hardcoded-hex/`any`-type cleanup (E24-style QA pass) as the next epic, since it's genuinely in-scope and unblocked. Everything else on the board is waiting on a person, not a developer.
