import StaffPicksManager from '../../components/admin/StaffPicksManager'

export default function StaffPicksPage() {
  return (
    <main id="main-content" style={{ maxWidth: '1440px', margin: '0 auto', padding: 'var(--space-8) var(--space-6)' }}>
      <StaffPicksManager />
    </main>
  )
}
